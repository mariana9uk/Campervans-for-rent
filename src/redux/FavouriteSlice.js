import { createSlice } from '@reduxjs/toolkit'
const handleAddFavorite=(state,{paylad})=>{
    
}

export const favoriteSlice= createSlice({
    name: 'favorite',
    initialState:{
favoriteItems:[],
    },
 extraReducers:(builder)=>{

 }   
})